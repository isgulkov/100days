#include <iostream>
#include <vector>

class union_find
{
    int total_components;
    int current_components;

public:
    union_find(int num_components) : total_components(num_components) { }

    bool same_component(int a, int b)
    {

    }

    void merge_components(int a, int b)
    {

    }

    int size()
    {
        return current_components;
    }
};

int main()
{
    int n;

    std::cin >> n;

    union_find u(n);

    
}
